//#include <ctype.h>  // Include ctype.h for character functions like toupper()
//#include <stdio.h>  // Include stdio.h for input/output functions
//#include <stdbool.h> // Include stdbool.h for using boolean variables

// Arav Halade
// 3/18/25
// HomeSales V3

/*
int main(void) {
    // static char salesIntial; // Declare a static character variable to store the salesman's initial
    // static int total; // Declare a static integer variable to store the total sales amount
    // static bool x = true; // Declare and initialize a boolean variable to control the loop
    // int salesP[3] = {0, 0, 0}; // Initialize array elements to 0

    // while (x) {
        // printf("Enter your Salesman initial: "); // Prompt user for salesperson initial
        // scanf(" %c", &salesIntial); // Read user input
        // salesIntial = toupper(salesIntial); // Convert input to uppercase

        // switch (salesIntial) {
        // case 'D':
            // printf("Enter the sale value: "); // Prompt user for sale value
            // scanf("%d", &salesP[0]); // Store sale value in first index
            // break;
        // case 'E':
            // printf("Enter the sale value: "); // Prompt user for sale value
            // scanf("%d", &salesP[1]); // Store sale value in second index
            // break;
        // case 'F':
            // printf("Enter the sale value: "); // Prompt user for sale value
            // scanf("%d", &salesP[2]); // Store sale value in third index
            // break;
        // case 'Z':
            // total = salesP[0] + salesP[1] + salesP[2]; // Calculate total sales
            // printf("Grand Total: $%d", total); // Print grand total

            // if (salesP[0] >= salesP[1] && salesP[0] >= salesP[2]) {
                // printf("\nHighest Sale: D"); // Print highest salesperson if D has the highest sale
            // } else if (salesP[1] >= salesP[0] && salesP[1] >= salesP[2]) {
                // printf("\nHighest Sale: E"); // Print highest salesperson if E has the highest sale
            // } else if (salesP[2] >= salesP[0] && salesP[2] >= salesP[1]) {
                // printf("\nHighest Sale: F"); // Print highest salesperson if F has the highest sale
            // }

            // x = false; // End loop
            // break;
        // default:
            // printf("Error, invalid salesperson selected, please try again \n"); // Handle invalid input
            // break;
        // }
    // }
    // return 0;
}
*/
